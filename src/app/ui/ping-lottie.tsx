import Lottie from "lottie-react";
import pingAnimation from "./animation_lmhbkf67.json";
type Props = {};

const PingingAnimation = (props: Props) => {
  return <Lottie animationData={pingAnimation} loop={true} />;
};

export { PingingAnimation };
